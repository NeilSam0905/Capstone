# USTore capstone - Prophet, deliberately separated
#
#   pip install -r requirements/requirements.txt -r requirements/requirements-prophet.txt
#
# NOT part of the standard install, and not needed by anything in the Part
# C checklist. Installing prophet pulls in cmdstan, which is a toolchain
# build that can consume a whole session and fail for environment reasons
# that have nothing to do with this project.
#
# That is exactly why model_benchmark.py runs eight methods without it:
# Block 6.2 recorded that Chapter 4's central result was unreproducible
# from the repo because it required this dependency. It no longer does.
#
# NOTHING IN THE REPO IMPORTS PROPHET ANY MORE. step4 (now
# step4_forecast_model.py) was the only file that did; it forecasts with a
# rolling mean and has been re-run, so `Result_Forecast(_Metrics)` is
# current and needs no part of this file. This requirements file is kept
# only so the Prophet path can be reinstalled if step4 is ever reverted -
# it is not needed to run the pipeline, the benchmark, or the tests.
#
# Version left unpinned on purpose: nobody has verified one here, and
# writing a number that has never been run would be worse than leaving it
# to resolve.

prophet
