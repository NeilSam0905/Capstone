// Power BI embed URL — set after publishing the .pbix (see README "Set up the Power BI dashboard").
// blank value since placeholder for now.
window.POWERBI_EMBED_URL = ""; // e.g. "https://app.powerbi.com/view?r=..."