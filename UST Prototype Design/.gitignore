# Build and dependency output — regenerated by `npm install` / `npm run build`
node_modules/
dist/
*.local

# Editor / OS noise
.DS_Store
.vscode/*
!.vscode/extensions.json

# NOTE: src/services/fixtures/*.json IS committed on purpose. They are
# generated (scripts/generate_fixtures.py) but the app cannot run without
# them, and regenerating needs ustore.db, which is gitignored.
